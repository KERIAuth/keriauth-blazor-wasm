namespace Extension.Services.SignifyService.Models {
    // Note: Schema is now defined in Schema.cs with full signify-ts compatible properties

    // Legacy definitions kept for reference:
    // public record A(string i);
    // public record Sad(A a, string d);
    // public record Status(string et);
    // public record ICredential(
    //     string issueeName,
    //     string[] ancatc,
    //     Sad sad,
    //     Schema schema,
    //     Status status,
    //     string? cesr
    // );
}
